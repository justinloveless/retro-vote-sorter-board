/** Global + optional per-tier override via tier_limits.featureFlags */
export const FEATURE_SUBSCRIPTIONS_ENABLED = 'subscriptions_enabled';
export const FEATURE_ORGANIZATION_SELECTOR_ENABLED = 'organization_selector_enabled';
/** Per-user localhost CLI advisor panel + settings (team poker) */
export const FEATURE_POKER_LOCAL_ADVISOR = 'poker_local_advisor';

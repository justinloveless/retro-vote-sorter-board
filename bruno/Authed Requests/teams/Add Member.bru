meta {
  name: Add Member
  type: http
  seq: 5
}

post {
  url: {{apiBaseUrl}}/api/teams/{{teamId}}/members
  body: json
  auth: inherit
}

body:json {
  {
    "userId": "{{memberUserId}}",
    "role": "member"
  }
}

settings {
  encodeUrl: true
}



meta {
  name: Send Notification (Admin Only)
  type: http
  seq: 3
}

post {
  url: {{apiBaseUrl}}/api/admin/notifications
  body: json
  auth: inherit
}

body:json {
  {
    "recipients": [
      {
        "email": "{{email}}"
      }
    ],
    "type": "Custom",
    "title": "Test from bruno",
    "message": "Does this work?",
    "url": null
  }
}

settings {
  encodeUrl: true
}

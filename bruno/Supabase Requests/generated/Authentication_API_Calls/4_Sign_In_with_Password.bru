meta {
  name: 4. Sign In with Password
  type: http
  seq: 4
}

post {
  url: {{supabaseUrl}}/auth/v1/token?grant_type=password
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  { "email": "{{email}}", "password": "{{password}}" }
}

meta {
  name: 36. Create Vote
  type: http
  seq: 36
}

post {
  url: {{supabaseUrl}}/rest/v1/retro_votes
  body: json
  auth: inherit
}

body:json {
{ "board_id": "string", "item_id": "string", "user_id": "string", "session_id": "string" }
}

headers {
  Authorization: Bearer {{accessToken}}
    apikey: {{supabaseAnonKey}}
    Content-Type: application/json
    Prefer: return=minimal
}

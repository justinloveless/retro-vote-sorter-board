meta {
  name: 15. Create Team
  type: http
  seq: 15
}

post {
  url: {{supabaseUrl}}/rest/v1/teams
  body: json
  auth: inherit
}

body:json {
{ "name": "string", "description": "string", "creator_id": "string" }
}

headers {
  Authorization: Bearer {{accessToken}}
    apikey: {{supabaseAnonKey}}
    Content-Type: application/json
    Prefer: return=minimal
}

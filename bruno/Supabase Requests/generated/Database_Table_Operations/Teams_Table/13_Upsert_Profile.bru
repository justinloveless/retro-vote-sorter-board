meta {
  name: 13. Upsert Profile
  type: http
  seq: 13
}

post {
  url: {{supabaseUrl}}/rest/v1/profiles
  body: json
  auth: inherit
}

body:json {
{ "id": "string", "full_name": "string", "avatar_url": "string", "role": "string", "theme_preference": "string", "background_preference": "any" }
}

headers {
  Authorization: Bearer {{accessToken}}
    apikey: {{supabaseAnonKey}}
    Content-Type: application/json
    Prefer: resolution=merge-duplicates
}
